# My neovim config

To start using, run the following command (Make sure you have backed up your config if you want to keep it):
```
git clone git@github.com:JulioSouza09/nvim-config.git ~/.config/nvim
```
